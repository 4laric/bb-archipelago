# Boss shuffle checkpoint

Work paused at the user's request for a reboot. PR #463 remains a draft.

The production registry has 193 of 462 directed routes. All 193 routes can
participate in a complete one-of-each, no-self 22-boss assignment. One Reborn's
six base-arena routes passed six direct native builds and six complete-roster
native builds; evidence is in the two `boss-one-reborn-donor-*-matrix.json` files.
Missing routes remain implementation gaps, not pairing bans.

This checkpoint also preserves completed adapter work that is **not registered
in the production shuffle yet**:

- Maria, Logarius and Gascoigne each support Gehrman, Moon Presence and Micolash
  through `arena_port.py`. Their nine original-input outputs compiled, and all
  nine direct native builds passed. Full-roster composition and registry
  integration remain pending. The new modules contain 11 tests, including two
  optional local original-input compile tests; the agent also passed 29 existing
  donor regression tests across the Maria and Logarius/Gascoigne checks.
- The Celestial Emissary destination adapter supports all six base combat
  packages. Eight contract tests and six original-input compilations passed.
  Direct native builds, full-roster composition, and registry integration remain
  pending. The original terminal and unrelated Ebrietas events remain exact;
  the replacement's actual death releases the hidden giant's terminal.

The collection floor includes these 19 new tests. Module execution floors
exclude only the three individually named optional original-input compile
tests, which are explicitly allowed in the AP skip ledger.

Continue with registry/full-roster integration for the nine arena-port routes,
then native validation and integration of the Celestial destination. Wet Nurse
destination expansion was planned but no implementation was started. Effect
bank conflicts and recursive FXR dependency semantics remain unresolved; static
probes are retained under the local `bb-boss-next-arenas/work` directory. No
selective effect filtering or compatibility bans have been introduced.

Standalone item/enemy randomization is separate draft PR #464, revision
343efe9. Its clean self-contained Windows package and original-input build/export
evidence are documented in `docs/standalone-windows-package-checkpoint.json` on
that branch. No game launches, mod activation, or save writes were performed.
